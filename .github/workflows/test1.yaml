name: my-first-workflow

on: push

jobs:
    my-first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v5
              
            - name: multi-line-commmand
              run: |
                echo 'Github Actions'
                ls -lrta
                cat README.md

            - name: Generate ASCII artwork
              run: cowsay -f dragon "Run for cover, I am a DRAGON...RAWR" >> dragon.txt

            # - name: checking files
            #   run: ls
            
            # - name: viewing files
            #   run: cat README.md

