name: my-first-workflow

on: push

jobs:
    my-first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v5
              
            - name: Test-message
              run: echo 'Github Actions'

            - name: checking files
              run: ls
            
            - name: viewing files
              run: cat README.md

